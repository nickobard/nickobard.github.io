

How to run server locally:

1. Go to project directory **personal-website**, i.e. `cd personal-directory`
2. run `npm run dev` to run development server

Once you made the changes with your project, just push the changes to master, the updates will be automatically deployed in a while. 